---
name: test
description: Run unit tests and report results
disable-model-invocation: true
allowed-tools: Bash
---

Run `zig build test` and report the result.

If tests fail, read the relevant source and fix the issue, then re-run tests.
