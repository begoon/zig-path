---
name: build
description: Build the project and report errors
disable-model-invocation: true
allowed-tools: Bash
---

Build the project with `zig build` and report the result.

If the build fails, read the relevant source lines and fix the errors, then rebuild.
